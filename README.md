# jarhq-messages-script

Make sure you have Nodejs installed.

First install dependencies with:

```yarn```

Add your details to the function call on line 24 in index.js

Then run index.js

```node index.js```
